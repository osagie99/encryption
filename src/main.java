public class main {
    public static void main(String[] args) {
        Encrypt encrypt = new Encrypt("Do you think you can fly with the way things are going");
        encrypt.encryptMessage();
        encrypt.decryptMessage();

//        Decrypt decrypt = new Decrypt();
    }
}
